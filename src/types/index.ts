export interface TeamMember {
  name: string;
  role: string;
  image: string;
}

export interface OpenRole {
  title: string;
  initialReview: string;
  process: string[];
  endLines: string;
  roleUrl: string;
}
